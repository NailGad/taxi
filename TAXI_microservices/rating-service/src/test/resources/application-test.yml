spring:
  jpa:
    hibernate:
      ddl-auto: create-drop
  datasource:
    url: jdbc:h2:mem:rating_test;MODE=PostgreSQL;DB_CLOSE_DELAY=-1

trip-service:
  url: http://127.0.0.1:9199

jwt:
  secret: test-secret-key-at-least-32-bytes-long!!
