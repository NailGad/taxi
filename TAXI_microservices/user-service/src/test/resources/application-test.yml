spring:
  flyway:
    enabled: false
  jpa:
    hibernate:
      ddl-auto: create-drop
  datasource:
    url: jdbc:h2:mem:user_test;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
  cache:
    type: redis

jwt:
  secret: test-secret-key-at-least-32-bytes-long!!
  expiration-ms: 3600000

taxi:
  cache:
    available-drivers-ttl-seconds: 60
